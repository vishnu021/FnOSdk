package com.vish.fno.manage.orderflow;

/**
 * OrderHandler - Interface for handling order execution and management
 *
 * Provides contract for processing trading orders within the strategy execution framework.
 * Concrete implementations should handle order placement, modification, and cancellation.
 */
public interface OrderHandler {
    // Define order handling methods as needed
}
